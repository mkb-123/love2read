import type { Level } from '../../lib/types';

// TODO: populate with ORT Stage 4 (Blue) words when she's ready.
// Add Deck objects to `decks` — no other code changes needed.
export const blue: Level = {
  id: 'blue',
  label: 'Blue — Stage 4',
  order: 2,
  color: 'text-blue-500',
  decks: [],
};
